{
  "compilerOptions": {
    "target": "es5",                    // Asegura que el código se compile a una versión compatible con la mayoría de los navegadores
    "lib": ["dom", "es2015", "esnext"], // Incluye las bibliotecas necesarias para trabajar con el DOM y características más modernas
    "allowJs": true,                    // Permite que los archivos `.js` sean procesados junto con `.ts` o `.tsx`
    "skipLibCheck": true,               // Omite la comprobación de tipos en las bibliotecas (esto puede mejorar el tiempo de compilación)
    "esModuleInterop": true,            // Asegura que las importaciones de módulos sean compatibles con ES6
    "jsx": "react-jsx",                 // Especifica que se usará JSX con React 17 y superior
    "moduleResolution": "node",         // Resuelve módulos de la misma manera que Node.js
    //"strict": true,                     // Habilita las comprobaciones estrictas de tipos
    //"noImplicitAny": true,               Impide el uso implícito de `any` en el código
    "forceConsistentCasingInFileNames": true,  // Asegura que los nombres de archivo sean consistentes con mayúsculas y minúsculas
    "resolveJsonModule": true,          // Permite importar archivos JSON
    "isolatedModules": true             // Asegura que los archivos `.ts` y `.tsx` sean auto-contenidos (importante para proyectos con Babel)
  },
  "include": [
    "src/**/*"                          // Incluye todos los archivos dentro de la carpeta `src`
  ],
  "exclude": [
    "node_modules",                     // Excluye la carpeta `node_modules` para no incluir dependencias en la compilación
    "build",                            // Excluye cualquier carpeta de salida como `build` o `dist`
    "public"                            // Excluye cualquier archivo público
  ]
}
